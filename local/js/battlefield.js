// Generated by CoffeeScript 1.6.3
(function() {
  var testData,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  testData = {
    monsters: ["qq"]
  };

  widnow.Battlefield = (function(_super) {
    __extends(Battlefield, _super);

    function Battlefield(game, data) {
      var monsters, _i, _len;
      Battlefield.__super__.constructor.call(this, game);
      data = testData;
      this.game = game;
      this.camera = new Camera();
      this.drawQueueAddAfter(this.camera);
      this.player = new BattlefieldPlayer;
      this.monsters = [];
      for (_i = 0, _len = data.length; _i < _len; _i++) {
        monsters = data[_i];
        this.monsters.push(new Battlefield);
      }
      this.initLayers();
      this.initMenus();
      this.setView("default");
    }

    Battlefield.prototype.initLayers = function() {
      this.mainLayer = new Layer(Res.imgs.bfForestMain);
      mainBg.z = 100;
      this.floatLayer = new Layer(Res.imgs.bfForestFloat);
      floatBg.z = 1;
      return this.camera.render(this.mainLayer, this.floatLayer);
    };

    Battlefield.prototype.initMenus = function() {
      this.menu = new Menu(Res.tpls['battlefield-menu']);
      return this.drawQueueAddAfter(this.menu);
    };

    Battlefield.prototype.initSprites = function() {};

    Battlefield.prototype.setView = function(name) {};

    return Battlefield;

  })(Stage);

}).call(this);
